## Packages
framer-motion | Page transitions and sleek futuristic animations

## Notes
Tailwind Config - Please ensure the following font families are added to tailwind.config.ts if possible:
fontFamily: {
  display: ["var(--font-display)", "sans-serif"],
  sans: ["var(--font-sans)", "sans-serif"],
}

The app is built with a dark mode "Space/Satellite" aesthetic. 
All color variables are defined in index.css using space-separated HSL values.
